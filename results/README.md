# Analysis outputs

Generated by:

```bash
python scripts/run_all.py
python scripts/validate_heat.py   # optional; also run from run_all
```

Outputs:

- `dataset_stats.json` — composition, coverage by sport/era, event breakdown, variance validation
- `heat_validation.json` — heat formula unit checks and empirical diagnostics

Re-run after updating `data/` CSVs.
